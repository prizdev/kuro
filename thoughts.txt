# Blender Server 3.0 - Project Kuro

main():

initArgs():
	# kuro -t 128932
	# maybe kuro -t 128932 -u username -p password -h host -s ftp/s3/sftp
	# taskname
	# ./taskname/model.obj
	# ./taskname/diff.jpg
	# ./taskname/spec.jpg
	# ./taskname/norm.jpg

importBlend():

importFiles():

initFiles():
	assignMaterial()
	assignTexture()

renderFrames():
	# let's see what happens!

processOutput(outputFilename):  #run once for sprite sheet, once for gif.
	setArgs():  #set args for following functions based on extension of outputFilename
	initImageMagick():
	initCanvas(sizeX,sizeY):
	importFrame():
		for frame in frames:
			loadFrame(frameSlice): # consider loadFrames option
			scaleFrame(scaleRes):
			animFrame(animBool):
			placeFrame(placeBool):
	exportCanvas(outputFilename):
	outputOptimization():  #pngQuant if outputFilename includes .png

#maybe

uploadToS3():

uploadToFTP():

uploadToSFTP():

deleteFiles():
